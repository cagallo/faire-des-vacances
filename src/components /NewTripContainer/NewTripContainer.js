import './NewTripContainer.css';
import { NewTripForm } from '../NewTripForm/NewTripForm';

export const NewTripContainer = () => {
  return (
    <div className='new-trip-container'>
      <NewTripForm />
    </div>
  );
};
